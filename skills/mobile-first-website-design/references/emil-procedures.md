# Emil procedure map

Resource gate: run `mise run validate` before using package files named here.

Backlink: [SKILL.md](../SKILL.md). Load when the named trigger occurs.

Apply the locked Emil Kowalski skill procedures through concise local contracts. Load the external skill when installed. If unavailable, use the matching fallback below and record `UNAVAILABLE_SKILL`.

| Procedure | Trigger | Required behavior | Fallback |
|---|---|---|---|
| `emil-design-eng` | visual implementation | deliberate hierarchy, spacing, type, color, and interaction decisions | native identity and judgement rules |
| `animate` | web motion | use transform and opacity where possible, interruptible interaction, reduced-motion parity | composition motion rules |
| `animate-expo` | Expo companion surface only | apply Expo-specific worklet and gesture patterns | `NO_MATCH` for web-only work |
| `animation-vocabulary` | motion specification | name easing, duration, choreography, continuity, and trigger precisely | fixed motion record schema |
| `apple-design` | premium direct-manipulation behavior | use clarity, deference, depth, feedback, and restraint when the brief supports it | audience-specific identity rules |
| `find-animation-opportunities` | motion planning | identify state change, causality, orientation, feedback, and delight opportunities before coding | native opportunity checklist |
| `improve-animations` | repair pass | audit purpose, continuity, timing, interruption, performance, and access | native motion audit |
| `prototype` | uncertain interaction | choose the smallest prototype that answers the uncertainty | throwaway HTML prototype |
| `review-animations` | motion release gate | inspect purpose, physics, continuity, hierarchy, performance, interruption, and reduced motion | native motion verdict |
| `pick-ui-library` | library decision | compare fit, bundle cost, access, theme control, maintenance, and lock-in | use platform primitives |
| `ask-sonner` | toast or transient feedback | use toasts only for nonblocking status; preserve inline recovery for errors | accessible inline status |

Do not add motion to compensate for weak hierarchy. Do not animate before the wireframe and identity hashes exist. Keep motion subordinate to comprehension and conversion. Validate it at every breakpoint, under reduced motion, and under interruption.
