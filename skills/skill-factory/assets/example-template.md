# Example: first run of {{NAME}}

Guess this example removes: what a complete run of this skill looks like, and what the executor pastes back to the user.

SCAFFOLD-PLACEHOLDER: every section below is a stub. Run the real commands, paste what they printed, and delete this line last.

## User says

```
{{DESCRIPTION}}
```

## Executor replies

SCAFFOLD-PLACEHOLDER: the fenced block below must hold the visible reply the user received, in full.

```
one paragraph of reply, then the evidence
```

## Commands run

SCAFFOLD-PLACEHOLDER: the fenced block below must hold real stdout and the real exit code of every command.

```
$ mise run info
{"name": "{{NAME}}", "description": "..."}
exit 0
```

## Files created

SCAFFOLD-PLACEHOLDER: list each file the run produced, its path, then its full contents in a fenced block. Write "none" when the run created no file.

## What the run proves

SCAFFOLD-PLACEHOLDER: name the observable result that shows the outcome is complete, and name anything the run left open.
