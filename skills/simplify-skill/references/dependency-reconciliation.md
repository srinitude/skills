# Dependency reconciliation

Parent and backlink: [`../SKILL.md`](../SKILL.md), Required composition.

The native source names host-only owners for lifecycle, authoring, profile repair, code cleanup, and behavioral checks. The public skill keeps their invariants without requiring unavailable private owners.

Use installed public peers for starting-point, human-behavior, agent-behavior, and meaning-preserving rewrite work. Internalize ownership, mutation, rollback, test-first, and package-proof gates for every other branch.

Do not copy private profile paths or tool names into the portable common path. Preserve them byte exact under evidence and map each clause to its portable owner.

If a requested branch still requires a missing exact dependency, return `BLOCKED_DEPENDENCY`. Do not invent a command, owner, or result.
