["rule:loop-contract"]
description = '''
# `rule:loop-contract`

## Why this runs

Use the seven stages without replaying finished effects.

## When to run

When starting or resuming a skill creation or maintenance operation.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

Start with the matching public command in [SKILL.md → Mise task graph](../SKILL.md#mise-task-graph).

Use the seven [SKILL.md stages](../SKILL.md#steps) for creation and maintenance: RECONCILED, SELECTED, READY, IMPLEMENTED, VALIDATED, FINALIZED and ADVANCE.

Each stage needs real Mise tasks, Mastra step IDs, declared inputs and outputs, direct dependencies and current proof.

Mise prepares runtimes and prerequisites.

Mastra owns domain state, waits and recovery.

It never calls nested Mise or its entry task.

Check prior proof without repeating completed effects.

Missing owners or failed guards block the steps that need them; written instructions alone are not working code.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:context"]
run = "node scripts/run_rule.ts rule:loop-contract"

["rule:reconciled"]
description = '''
# `rule:reconciled`

## Why this runs

Bind the current request, sources and rights.

## When to run

At the start of an operation, or when governing inputs change.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

1. **RECONCILED.** Model: follow the full load and review rules.

Bind the command, outcome, audience and scope.

Bind accepted behavior, fixed rules, methods and proof.

Host: check identity and rights.

Model and [Mise](https://mise.jdx.dev/): inspect the sources and package.

Check the environment, interfaces, callers and graph.

Run doctor and each policy task that applies.

Keep exact versions, modes, source bytes and baseline.

Missing or changed sources block work that needs them.

Never rewrite sources to hide drift.

Use scope/use-case owners to map load, work and acceptance routes.

Check source coverage, file roles, links and body duties through the ledger.

Keep parent records.

Complete the whole-body foundation and fresh candidate review.

Exceptions need approval and named pending checks.

Human clarity and same-meaning proof stay required.

Explore within fixed gates.

Ask only for material facts that cannot be found, or required rights.

Return help/doctor results; otherwise keep the chosen operation.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:loop-contract"]
run = "node scripts/run_rule.ts rule:reconciled"

["rule:selected"]
description = '''
# `rule:selected`

## Why this runs

Choose the earliest ready gap from SKILL.md outward.

## When to run

After reconciliation, when choosing the next unresolved implementation gap.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

2. **SELECTED.** Model: choose a named open source duty and show the gap in its code or instructions.

Start at SKILL.md, then files one connector away, then two, and so on by shortest valid path.

Follow real prerequisites and approved exceptions.

Resume eligible unfinished work before other preparation.

Use the existing adaptation policy to break ties.

A missing path is a defect, not a reason to invent a layer.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:reconciled"]
run = "node scripts/run_rule.ts rule:selected"

["rule:ready"]
description = '''
# `rule:ready`

## Why this runs

Bind the exact allowed change and deciding checks.

## When to run

After selecting a gap, before any related file or domain effect.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

3. **READY.** Model and [Mise](https://mise.jdx.dev/): fix the ordered plan before acting.

Name its inputs, owners, resources, effects, readbacks, tests, recovery and needed changes in other files.

Use `plan-standardize`, keep `baseline_digest`, and use `task-graph-policy`.

Imports also need a source-corpus audit.

Follow the exact reviewed branch for standardization and variants.

Check collisions, ownership, layers and real prerequisites.

Bind each file's identity and lineage to its current and planned typed path to SKILL.md, distance, body rule, need, intended effect and deciding check.

Check rights, required reads and allowed changes before each effect.

Once ready, make the change before optional preparation.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:selected"]
run = "node scripts/run_rule.ts rule:ready"

["rule:implemented"]
description = '''
# `rule:implemented`

## Why this runs

Carry out the reviewed change and retain its effects.

## When to run

Once the selected operation passes its required readiness checks.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

4. **IMPLEMENTED.** [Mastra](https://mastra.ai/docs/workflows/overview) executes the domain plan with real scripts, allowed runners and assigned model or human judgments.

Follow the task routes in [SKILL.md → Evidence](../SKILL.md#evidence) for the per-file reads, graph review and change history.

For new skills, build tasks and CI/graph contract tests first.

Observe a failing behavior test, write the smallest sound change, then update docs and evals.

Use focused RED, GREEN, REFACTOR and affected regression checks.

Reject placeholders, generic profiles, unsupported interfaces, lost behavior and source changes without authority.

Keep exact before/after bytes, modes, diffs and observed effects.

Keep each failed attempt.

If an effect is unclear, inspect it before retrying.

A receipt or run ID does not prove that an effect happened just once.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:ready"]
run = "node scripts/run_rule.ts rule:implemented"

["rule:validated"]
description = '''
# `rule:validated`

## Why this runs

Check actual behavior and invalidate affected proof.

## When to run

After an implementation effect, before relying on its result.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

5. **VALIDATED.** Model: run the assigned checks and obtain required real human or recipient proof.

Run `mise run decision-policy`, the target's full `mise run ci`, `validate-target`, `eval-target` and the actual case runner.

Inspect exact artifacts in their real media.

Check source support, domain effects, required non-effects and safe recovery in independent outputs.

A factory pass cannot hide an output failure.

Fix the lowest failed owner and mark affected dependent proof stale.

Bind proof to exact resulting inputs; keep them unchanged while checks run.

Complete the post-read, body integration, file-path and preservation checks.

Reject stale, unrelated or unsupported proof.

Approved prerequisite work leaves missing integrated or human proof pending.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:implemented"]
run = "node scripts/run_rule.ts rule:validated"

["rule:finalized"]
description = '''
# `rule:finalized`

## Why this runs

Review the whole changed file and its known consumers.

## When to run

After affected validation, before treating a changed file as ready for delivery.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

6. **FINALIZED.** Model: review each changed file as a whole.

Check its source duties, known callers, downstream needs, Markdown clarity, physical size/depth limits and graph/ledger fit.

Keep enough content to meet those needs.

Reject needless layers, duplicate owners, cosmetic churn and speculative additions.

Hardening proves the current contract, not that a file will never need repair.

Pending proof is still not acceptance.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:validated"]
run = "node scripts/run_rule.ts rule:finalized"

["rule:advance"]
description = '''
# `rule:advance`

## Why this runs

Record results, remaining work and verified delivery.

## When to run

After file finalization, when recording progress or moving toward delivery.
This rule binds this factory and every skill it creates or updates.

## Inputs

Read [SKILL.md](../SKILL.md), the current request and the sources named below.
Use the same bound request and private state as `rule:context`.
Include the exact relevant files and prior evidence in the request context.
A dependency pass proves its checked scope, not all skill acceptance.


## Work

7. **ADVANCE.** Model: record each public task's real run with `mise run invocation-policy -- <receipt>`.

Any domain non-use needs source support.

Apply the improvement and prior-adoption checks in [SKILL.md → Evals](../SKILL.md#evals).

Trials need a real need, rights, accepted required work, full proof and a safe restore budget.

After outcome and acceptance work, run `mise run mise-primitives-update` last through its reviewed catalog/lineage branch.

First its prior task updates [Mise](https://mise.jdx.dev/), with no plugins.

Next it refreshes the schema catalog for that exact release.

Its post-task requires domain review and lineage refresh.

Rerun full CI with that binary.

Check exact local, installed and authoritative remote contents.

Check discovery, invocation, independent outputs and CI for that revision.

Honor release rights.

Before a PR merge, pass all non-merge proof and obtain actual merge approval.

Then verify main and final delivery.

Keep all other acceptance duties.

Use the ledger and the progress rules in [SKILL.md → Evals](../SKILL.md#evals) to record effects and remaining work.

Read the full workflow output. Use the model's own tools and judgment.
Code checks facts that can be tested; required human work stays with its owner.
Do not run this task or its dependents from inside this work unit.
Missing inputs or proof keep the affected result pending.
Correct the input or obtain the required proof before retrying.

## Proof

Return reasons, source locations, actual evidence and remaining limits.
Keep the reply bound to this task and its exact inputs.
Resume this same task through Mise using the `rule:context` reply contract.
A workflow pass checks bindings; it cannot prove meaning or human acceptance.
'''
env = { UV_PYTHON = "{{tools.python.path}}" }
depends = ["rule:finalized"]
run = "node scripts/run_rule.ts rule:advance"
