# Eval guide

Load this file when a person grades an eval run.

Give one point for each fact:

- The intake has all six main fields.
- Current sources and seen views are named.
- Each atom points to passing token proof.
- No part skips the layer chain.
- A low change makes linked high parts stale.
- The state set stays open and records context and transitions.
- Scripts create and check the eye, brain, and touch checklist.
- The model answers every review item and makes each judgment.
- Every check uses the full open eye, brain, and touch sweep.
- Every objective failure and harm item has a model answer.
- Objective failures use direct render and input proof.
- Each check keeps its exact question, probe order, proof, and decision rules.
- The reply does not turn code proof into sight proof.
- Safety, access, sense, and agency vetoes hold.
- Public prose passes the reading rules.
- The final state is `PASS`, `STALE`, or `BLOCKED`.

The case passes only with sixteen points. This score tests acts. It does not rate taste.
