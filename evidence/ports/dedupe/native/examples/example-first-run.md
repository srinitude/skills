# Example index

Load the file matching the command:

- `examples/help.md` for capability discovery
- `examples/inspect.md` for a normalized list report
- `examples/apply.md` for an approved in-memory mutation
- `examples/failure.md` for an unsupported adapter

Each example separates user intent, action, evidence, and remaining authority.
