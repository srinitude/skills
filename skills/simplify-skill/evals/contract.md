# Evaluation contract

Run all ten source-bound cases under `without_skill` and `with_skill` with two repetitions. A case passes only when every required marker appears, no veto marker appears, and the decision matches the case contract. Keep source-case identity, fixture results, speed results, and any later model sweep separate.
