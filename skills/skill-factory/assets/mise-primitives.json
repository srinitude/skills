{
  "version": "1.0.0",
  "skill": "skill-factory",
  "catalog_version": "2026.9.3",
  "groups": {
    "config": {
      "used": ["env", "min_version", "settings", "task_config", "tasks", "tools"],
      "not_applicable": ["_", "alias", "bootstrap", "deps", "dotenv", "dotfiles", "env_file", "env_path", "history", "hooks", "monorepo", "monorepo_root", "oci", "plugins", "redactions", "shell_alias", "task_templates", "tool_alias", "tool_config", "vars", "watch_files", "wrappers"],
      "used_reason": "skill-factory uses the Mise task graph, a Python toolchain, a minimum runner contract, bounded parallel jobs, artifact caching, a fixed task root, and deterministic Python environment values to produce portable agent skill packages quickly and correctly.",
      "nonuse_reason": "The remaining top-level Mise features do not improve this single-package agent skill factory result, or they introduce remote services, secrets, shell state, repository scale, or side effects that this invocation does not own. Dotfile history is outside agent skill package maintenance; Git lineage and invocation receipts already own change evidence.",
      "creative_use": "The agent skill environment disables bytecode cache files while bounded Mise concurrency and content-addressed static-check caches shorten repeated proof without caching live judgment.",
      "evidence": "mise.toml, generated mise template, speed-contract tests, and plan-standardize runtime-cache tests."
    },
    "task": {
      "used": ["alias", "cache", "depends", "depends_post", "description", "env", "output", "outputs", "raw_args", "run", "run_windows", "sources", "timeout", "usage"],
      "not_applicable": ["allow_env", "allow_net", "allow_read", "allow_write", "confirm", "deny_all", "deny_env", "deny_net", "deny_read", "deny_write", "dir", "extends", "file", "hide", "interactive", "pass_through_env", "quiet", "raw", "rust_cache", "shell", "silent", "tools", "vars", "wait_for", "watch"],
      "used_reason": "skill-factory uses explicit task metadata, dependency edges, bounded execution, declared sources, empty-output check caches, task-local binding to Mise’s resolved Python installation for isolated uv helpers, portable Python tools, native required review arguments before lineage prerequisites, platform commands, and prefixed evidence for agent skill operations. Native raw_args preserves standardization arguments, including help and literal separators; its actual Python and Usage consumer retains validation rather than trusting forwarding.",
      "nonuse_reason": "The unused task fields would add interactive locks, hidden evidence, sandbox claims, file-task indirection, Rust behavior, or watchers that do not advance this agent skill package operation.",
      "creative_use": "Agent skill static checks cache only successful no-output evidence while live research, model judgment, external targets, mutation, and lineage stay uncached; timeouts bound stalls and native required arguments reject missing lineage reviews before prerequisite work. The maintenance post dependency does not prove successful parent execution or replace per-file review.",
      "evidence": "mise.toml, generated mise template, speed-contract tests, task graph tests, timeout failures, cross-platform command tests, and runtime-task dependency tests for helper isolation and preservation of custom domain environments. Standardization Usage tests verify current metadata, preserved values, rejected stale definitions and recovery."
    },
    "task_config": {
      "used": ["dir", "includes"],
      "not_applicable": ["cache", "cascade", "excludes", "global_env", "global_inputs", "global_pass_through_env", "input_groups", "rust_cache", "shell"],
      "used_reason": "The agent skill fixes tasks to the package root and includes an exact list of local TOML rule owners. Native Mise discovery must agree with those sources.",
      "nonuse_reason": "The agent skill needs no descendant config search, excluded task sources, safe artifact-output cache, Rust cache or host-shell override. Explicit includes replace the old single-file assumption.",
      "creative_use": "The agent skill splits full rule bodies by purpose while one native include list and declared task edges preserve ownership and execution order.",
      "evidence": "mise.toml task_config, task_definitions.py and the real grouped-task and task-tool tests check exact includes, source drift, symlink rejection, dependency order and recovery. Human and domain acceptance remain separate."
    },
    "tool": {
      "used": ["version"],
      "not_applicable": ["depends", "install_before", "install_env", "lazy", "lazy_bins", "minimum_release_age", "os", "path", "postinstall", "prefix", "ref", "version_order"],
      "used_reason": "skill-factory declares Python and uv versions for deterministic agent skill checks and cached dependency resolution. The factory alone pins Usage 6.8.0 for its native argument projection and checks.",
      "nonuse_reason": "The Python registry already supplies stable ordering and portable artifacts, so source refs, local paths, prefixes, install ordering, release-age delay, and operating-system filters add no skill-factory value.",
      "creative_use": "The agent skill graph lets uv reuse its pinned YAML parser cache in isolated helper environments tied to the resolved Mise Python path, with ancestor-project discovery and interpreter downloads disabled.",
      "evidence": "mise.toml tool declarations, malformed-frontmatter attack, and doctor version receipts."
    }
  }
}
