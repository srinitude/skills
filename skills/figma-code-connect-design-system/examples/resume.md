# Resume example

## Request

Resume `/work/runs/settings/run.json` after the input component changed in code.

## Output

```text
STATUS: STALE
Changed source: Input export and props at the current repository revision
Invalidated: Input Code Connect mapping template, field molecule, settings-form organism, settings UI composition template, default and error screens
Still current: DTCG tokens, unrelated atoms, navigation organism
Resume point: harden the Input atom against the new code API, then rebuild affected dependents in order
External changes during resume inspection: none
```

Resume is based on current target readback, not the last narrated step.
