# Help example

## Request

`figma-code-connect-design-system help`

## Output

```text
Commands: help, plan, run, validate, resume, refresh-sources
Inputs: FigJam, PRD, image, codebase, Figma design file, published library
Modes: create, update, auto
Hierarchy: DTCG tokens -> atoms -> molecules -> organisms -> UI composition templates -> screens
Writes: none for help; every later repository, Figma, publish, unpublish, delete, package, GitHub, or CI effect is separately gated
```

No files or external systems change.
