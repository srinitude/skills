# Current research protocol

Read this file in Step 08 before a current source changes a token, experiment, technology record, or claim. It owns dated research capture for `assets/creative-source-frontier.json`. Use `mise run token-packet -- <args>` for this routed resource.

## Start with one date

Acquire one fresh local date anchor. Save it with the run. Recheck the DTCG publication, active standards, font ranking, comparison corpus, current technology, and time-sensitive claims against that date.

## Search the required range

Use current primary sources, related domains, structurally distant domains, one antithetical source, relevant recent papers, and the skills.sh `find-skills` path for method discovery. Treat search results and popularity as leads, not proof.

## Record each source

Save query, URL, publisher, publication date, retrieval date, source class, version, extracted mechanism, counterevidence, and access limits. Open the primary source before transfer. A current preprint may propose an experiment but cannot alone settle a stable rule.

## Account for available paths

Record every source-relevant web path available to the runtime. Mark it `used`, `not_applicable`, `unavailable`, or `blocked` with a reason. Render a source page when its claim depends on script-only state or visual layout.

## Stop honestly

Pass only after frontier quotas and the unchanged-addition stop rule pass. No finite search is called exhaustive. Return `E_SOURCE_CURRENT` or `E_SOURCE_DISTANCE` when evidence or access cannot support the required boundary.
