Command palette overlay in a keyboard launcher, sitting over a blurred desktop. A search field runs across the top, then grouped results labelled top, pinned and unpinned, each with a count and each row showing a favicon, a title and a grey url. A secondary actions panel floats over the right of the results list, listing open in this browser, open in a small window, open in a new window, open in an incognito window and open in a space, each with keyboard glyphs, and its own search field at the foot reading search for actions. Two rows appear highlighted at once, one in the results and one in the actions panel. The footer strip repeats the primary action and shows the shortcut for opening the actions panel.
