# Evaluation contract

Run all six source cases under `without_skill` and `with_skill` with two repetitions. Pass only when every required outcome appears, no veto appears, and the decision matches. Keep fixture, package, and live results separate.
