# Evaluation rubric

Pass when the response names actor, task, cost, invariants, and loss budget; follows the closed state machine; preserves protected spans; uses complete ledger coverage; applies the correct veto; and separates component, package, and whole-skill status.

Fail on line-count-only acceptance, silent loss, invented dependencies, unscoped behavior claims, missing tests, or child extraction without ownership and graph proof.
