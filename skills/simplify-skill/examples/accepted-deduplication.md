# Accepted deduplication

Two clauses share owner, strength, conditions, exceptions, and behavior. Map both source IDs to one target, prove equality, rerun the same task, and accept only when a declared retrieval or maintenance cost falls.
