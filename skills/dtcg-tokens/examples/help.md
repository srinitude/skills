# Help command

**Guess removed:** The user does not need to know the command grammar before asking.

## User

> What can dtcg-tokens do?

## Visible reply

`dtcg-tokens` accepts `help`, `generate <inputs>`, `validate <tokens>`, and `prove <tokens> <evidence>`. Generation accepts text, documents, data, code, images, screenshots, video, audio, live interfaces, and mixed sources. It creates DTCG 2025.10 JSON, evidence JSON, a run-specific standalone HTML proof, and a run record. Every final token file contains at least three experimental tokens from three different exploration strategies, including one inversion or antithesis. Typography generation captures the live Google Fonts catalog on the run date, rejects the most popular 50%, visually compares at least three eligible source-fitting families, and embeds the selected WOFF2 files and licenses into the offline proof. Visual input and proof artifacts require native visual inspection. Originality is provable only inside a named comparison corpus. Global uniqueness is outside this skill's valid claim scope and remains unproved.

No command ran and no file was created. Exit codes do not apply.
