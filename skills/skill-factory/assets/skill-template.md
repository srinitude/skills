---
name: {{NAME}}
description: "{{DESCRIPTION}}"
license: MIT
metadata:
  author: Kiren Srinivasan
  version: "0.1.0"
  scope: "{{SCOPE}}"
---
# {{NAME}}

## Outcome

SCAFFOLD-PLACEHOLDER: define the observable {{NAME}} result, recipient, domain, supported commands, forbidden outcomes and completion evidence in this paragraph. Replace every seed with source-backed domain content before acceptance. The unit of acceptance is the complete working capability: instructions, resources, configuration, code, workflows, tests, examples, evals, integrations, outputs and maintenance. Preserve purpose, accepted behavior, source bytes, scope, audience, lineage, independent variants, unrelated work and existing authority. Apply this body contract whenever the skill is invoked, generated or updated, and propagate it when this skill itself creates or updates skills. Passing checks without serving the intended recipient is unfinished.

**Start here.** Read this whole body first. It owns the core operating rules and the reusable ledger method. This skill owns its source ledger and review history. Build that ledger from this skill's source instructions and the portable factory rules that apply. Never copy another task's ledger or approvals just to pass a check. At first and later loads, capture all of this skill's current ledger and governing input bytes. Establish semantic review coverage for every current rule and relationship. Read declared initial resources in their dependency order before use. Reuse only completed reviews whose exact source, full scope and relevant context remain valid. Check affected callers, dependencies and evidence; review every changed or affected record. A fresh builder must perform any review it cannot validly reuse. Missing or stale coverage blocks its consumer. The [use-case contract][use-case-contract] owns the resolved outcome, audience, ledger and matrix bindings; the [lifecycle record][lifecycle-record] owns resource roles and loading conditions; the [decision record][decision-record] owns choices and judgments. Missing or ignored required content blocks its consumer. A path, link, digest, previous load or model acknowledgment does not prove actual consumption. The linked records are checked through `mise run use-case-policy`, `mise run primitive-lifecycle-policy` and `mise run decision-policy`; check execution does not replace their required loading. The invoking agent remains responsible for the integrated result after any delegated work.

**Scope and audience key.** This skill has {{SCOPE}} scope. Every created or updated skill has exactly one string `metadata.scope`: `user` means intended availability across the user's projects; `project` means availability for a particular project or repository. Store the main result consumer once as `audience.primary: human` or `agent` in the owning use-case contract, independently of scope and executor. Derive body and report labels from those canonical fields. Preserve valid values unless the user requests a change or a purpose conflict is resolved. Retain secondary and output-specific audiences when they change requirements. A human result can be produced by an agent; an agent result can have a human report.

Generic wording, installation and registry location cannot decide scope or audience. Resolve material missing or conflicting designation before affected writes; reject duplicate keys, wrong types and invalid values. Untouched legacy inspection can permit absent scope or audience without accepting a changed output. Classification grants no installation, relocation or permission authority; use the [scope contract](references/skill-scope-contract.md) through `mise run validate` for resolution and authorized placement.

| Key | Action and evidence meaning |
| --- | --- |
| Human result | A person uses or experiences the main result. Test relevant task, comprehension, learning, accessibility, quality or experience outcomes with the actual evidence each claim requires. |
| Agent result | A model or agent consumes the main result. Prove the interface, real downstream semantic use and failure handling; retain material later human consequences. |
| Operation | Name the actual reader, orchestrator, executor, reviewer and authorized decision owner, with required inputs and permitted effects. |
| Mechanical proof | A program establishes its named predicate over captured inputs, with result, coverage and limits. |
| Model assessment | An identified capable model inspects the actual artifact using declared evidence, modalities and criteria. It is not human evidence. |
| Human evidence | A real person supplies the specified preference, observation, expert review, task result or permission. These kinds are distinct. |
| Required or variable | Prerequisites and acceptance gates must hold. Bounded options and open creative choices remain available within the contract. |

**Owners before action.** Inventory the actual humans, models, invoking agent and runner, orchestration tools, programs, runtimes, libraries, services, operating environment, stores, evaluators and relevant resource authorities. Do not invent absent participants or capabilities. Instructions, skills, workflows, schemas, evidence and matrices are owned resources; their presence grants no authority. Assign one effective owner for each definition, execution, permission, data/state, proof/acceptance and maintenance responsibility, with canonical defaults and explicit exceptions in the existing records. For reading, writing, acting, judging, verifying and validating, state who, what, when, how, where and why: source/version and trigger; author versus persistence tool; exact input, target and allowed effect; mechanism or criteria; result and next owner; failure, recovery and acceptance authority. Several explicit roles may belong to one actor, but vague shared ownership, competing writers and missing handoffs fail. Keep private host identities and paths at the integration owner.

## Motivation

A useful skill carries the work through to its recipient's next action. Freeze the intended user, task, starting conditions, artifact or state, boundaries, forbidden outcomes, mandatory methods and adequate proof before implementation. Give every material rule, primitive and decision its domain role, concrete progress value, reason, prevented failure and falsifier. Preserve correctness, intelligence, creativity, craftsmanship, judgment, taste, grounding, proof, validation, safety, scalability and applicable accessibility or other promised qualities. Define those qualities for the actual domain; branding, confidence, file counts, polish, activity and aggregate scores cannot establish them or compensate for a mandatory failure.

Use the smallest coherent structure that preserves the whole contract. Keep one canonical owner per rule, one stable term per concept, one default route per supported situation and explicit necessary failure branches. Remove demonstrated duplication, obsolescence, unused resources and speculative layers without merging different meanings or stripping necessary domain context. Set context and resource budgets for the operation, while retaining every required full read. Read the [writing rules](references/writing-rules.md), checked through `mise run lint-writing`, for plain, active, precise language: result first, defined terms, one idea per sentence and one topic per paragraph, in dependency order. Move optional detail only with its destination, owner, exact load trigger and backlink. Counts and lint are diagnostics. The generation contract requires same-meaning human review of clarity and completeness; model assessment does not replace that evidence.

Prevent WorkSlop: apparently completed work that fails its purpose and transfers avoidable interpretation, verification, repair or completion to the recipient. Establish recipient, purpose, context, evidence and next action. The producer checks facts, sources, calculations, behavior and integration; marks drafts, assumptions, decisions and compatibility honestly; and preserves legitimate human review and creative iteration. Do not reward output volume, adoption, indiscriminate tool use, pleasant fluency, detector scores or reduced questions instead of useful outcomes. Ground adopted rules in the [domain research owner][domain-research-owner], including the [WorkSlop research](https://www.betterup.com/workslop), [preprint](https://doi.org/10.31234/osf.io/5f78h_v1), [survey](https://zapier.com/blog/ai-workslop/) and later [conditions research](https://www.betterup.com/the-case-for-conditions); distinguish self-report, association, causal evidence, commercial advice, measured costs and estimates, and seek contrary evidence.

**Portable package contract.** Keep one owned directory whose name matches the frontmatter. Ship its full domain body, public task graph, CI, worked examples and declared support. Parse YAML; reject duplicate keys and wrong types. Preserve scope. Use the [generation contract][generation-contract] through `mise run validate` for field/name rules, author, license and independent versions. Keep the body at most 200 physical lines and 100000 characters. Wrap Markdown for clear reading. Keep prose blocks at most 150 words, with needed blank lines. Never pack text to meet the line cap. Put optional detail at a linked owner; keep core rules here. Name each resource’s public task in its prose block or section. Implementation links name owners, not commands. Run `mise run lint-writing` after prose changes and the required Markdown review before acceptance. Routing and lint do not prove loading, meaning or reading ease.

## Evidence

**Source and reading contract.** Use the user's current instructions and live authoritative domain/project sources, then the full current target and [generation contract][generation-contract], applicable reference owners, real examples and eval evidence. Reconcile accessible history in chronology, retaining provenance and the newest repeated instruction while preserving compatible conditions; report inaccessible history. Read the complete relevant package and actual callers/copy paths before changing it. Use `mise run validate` for the generation contract and `mise run domain-research-policy` for domain evidence checks, with the actual resources loaded by the assigned reader.

Preserve immutable source bytes and original definitions. Keep facts, historical observations, examples, hypotheses, quotations and tool outputs distinct from instructions. No index, summary, count or checklist replaces the source or removes an unindexed obligation. Preserve exact meaning, force, quantifiers, negation, conditions, exceptions, motivations, roles, mandatory methods and proof. Permitted non-use needs the source's condition, concrete evidence and responsible review; cost, difficulty, missing capability and unknown evidence are not non-applicability. Keep file baselines and history separate from current package observations; recorded presence is not live existence proof. Use [source checks](scripts/review_ledger_source.py) through ledger actions `check-capture` for recorded bytes/locators and `check-sources` for separately supplied document/original bindings and the frozen source inventory. Establish those bindings' authority and semantic coverage separately.

**Reusable ledger artifact, owned here.** Extend the existing use-case, source-mapping, lifecycle, decision, invocation and eval records; do not create a second supervisor or per-sentence framework. Retain complete authorized source content and exact provenance, stable parent/component IDs, byte/line/field locators, quotations, context, table columns, examples and linked definitions. Every material rule or detail carries its theme, characteristics, intended meaning, rule type and strength, expectation, purpose and motivation, applicability and exceptions, identity/provenance, scope/audience, parties and activities, input/output, dependencies and ordering, authority/privacy, time/validity, state/lifecycle, software contract, resources/efficiency, quality/creativity, study/work concepts, proof/judgment, failure/recovery, interactions, propagation, body impact, initial loading, work decision and delivery. Reuse canonical defaults with explicit exceptions; missing meaning stays unresolved. Requirements, prohibitions, permissions, conditional rules, optional methods, defaults, invariants, definitions, facts, rationales, examples, hypotheses, observations, limitations and open questions retain their distinct force.

**Relationship records.** Preserve every source-grounded relationship within and across sources, rules, details, files and actual consumers. Bind exact endpoints, direction, context, condition, meaning, source evidence, reviewer and current state. Retain this complete versioned catalog, adding newly found distinct relations with definitions and source support before use. Ordered multiple endpoints, repeats, grouping, roles, higher-order interactions, alternatives and valid feedback must survive. Confirmed relationships and unreviewed candidates are separate. Absence proves neither independence nor a favorable state; no edge is automatically transitive, causal, executable or an acceptance decision. Definition changes invalidate affected relationship reviews unless a verified explicit override preserves their effective meaning. Data structures, algorithms and schemas must preserve valid unanticipated reasoning and solutions; otherwise use the verified complete authorized baseline or block only the affected consumer without weakening validation or acceptance.

| Relationship family | Distinctions the ledger and every traversal preserve |
| --- | --- |
| Source and meaning | `part-of`, `contextualizes`, `defines`, `references`, `equivalent-under`, `distinct-from`, `refines`, `example-of`, `qualifies`, `excepts`, `contradicts`, `supersedes`, `preserves`. Preserve the exact parent, scope and authorized exception; similar words do not prove equivalence. |
| Logic and purpose | `requires-all`, `requires-alternative`, `requires-exclusive-choice`, `conditional-on`, `forbids`, `permits`, `must-be-absent`, `motivates`, `advances`, `prevents`, `protects`, `tradeoff-with`, `reinforces`, `interacts-with`. A preferred route is not a prerequisite; neither a tradeoff nor an example waives a mandatory rule. |
| Order and availability | `read-before`, `executes-before`, `gates-effect`, `gates-acceptance`, `remains-available`, `exclusive-access`, `feedback-to`, `parallel-with`. Keep reading/design, executable scheduling, protected effects and acceptance separate; preserve native conditions and versioned feedback. |
| Owners and effects | `definition-owned-by`, `execution-owned-by`, `authorized-by`, `state-owned-by`, `accepted-by`, `maintained-by`, `reads`, `writes`, `consumes`, `produces`, `hands-off-to`, `bound-to`. Required responsibility and observed performance are different claims. |
| Proof and revision | `proves`, `falsifies`, `calibrates`, `invalidates`, `recovers`, `restores`, `tests`, `compares-with`. Evidence establishes its particular claim under its conditions; a test's existence or a supplied verdict is not proof. |
| Body and distribution | `generates`, `copies-from`, `adapts`, `independent-variant-of`, `refreshes`, `body-represents`, `change-reviews-body`, `initially-loads`, `conditionally-loads`, `delivers`. The source body represents its target subjects' core contract and routes detail. Matching copies can both be stale. |
| Concepts and composition | `alias-of`, `broader-than`, `narrower-than`, `related-concept`, `identical-to`, `approximately-corresponds`, `causally-influences`, `higher-order-interaction`, `selected-from`. Separate taxonomy, exact/approximate identity, causal influence, workflow order and evidence. A pairwise projection cannot replace a joint interaction. |

**Traverse, work and check.** Use one public ledger entry through the [task graph][task-graph] and a real [Mastra workflow][mastra-workflow] at the existing domain-operation boundary. The workflow must load and validate the complete current ledger/body inputs, expose exact source and context, all semantic facets, typed incoming/outgoing relationships, traces, bounded candidate pairs and finite selections, dependency-ordered file impacts and current work, then record supplied judgments and actual evidence through the canonical owners. Continue explicit pages until the selected space is exhausted; never label a sample complete. Reading, source capture and structural checks do not supply a model or human judgment. Before claiming this route works, prove actual loading, work, guarded recording, rejection and valid recovery through the public entry; unavailable integration blocks its dependent acceptance.

Invoke `mise run ledger -- <request.json>`. Declared source/reading/task edges, candidate pages, work context and file impacts preserve scope beside unchanged assertions. Keep coverage explicit; missing required capabilities block acceptance. The [file writer](scripts/review_ledger_write.py) through `mise run ledger` guards caller-scoped file creation/replacement; ordinary writes require `initial_body_review`, a separate path/digest binding to the installed body’s current initial review, captured and checked before and after the write; do not combine it with bootstrap or body-revision inputs; body changes require the explicit `body_revision` review; an optional `change.mode` binds the expected permissions (or absence) and new ordinary permission bits, including mode-only changes; unchanged/default mode behavior remains preserved and stale or privileged-bit requests fail; it accepts `bootstrap_body` with explicit `body` and `review` file bindings only before the target body exists; factory scaffold creation and standardization staging also use this writer after a complete current plan, initial-body review and all per-file declarations pass. Standardization binds exact target bytes, file and directory permissions, profile bytes, factory files and any tracked-text baseline. Its native formatter plan retains final bytes, supported/ignored-parser observations and current configuration, Node binary and formatter-package identities; dynamic configuration/plugin imports still require their own dependency review. The pinned formatter is a declared package dependency for independent tests. Unchanged reruns still validate mappings and policy records. Promotion rechecks inputs under the shared package lock, retains excluded Git/runtime state and directory layout/permissions, and validates final bytes, modes and current sources before discarding the backup. A failed final check restores the prior package; failed automatic restoration retains both directories with a specific recovery error. These cooperating-writer guarantees are not hostile-writer or process-crash isolation. Variant publication first exposes every future file, including lineage, through `accept --preview`; applying requires the saved `--plan-file` and a current `--ledger-review` in addition to the separate domain `--review`. The same initial-body and per-file gate covers every staged variant file. The publication plan also lists every owned target file absent from the future package as `retirements`, with its prior bytes and mode. The per-file ledger review must cover those paths as well as future files. Plan retired consumers before their retired providers using the observed local Python import graph; validate all semantic removal relationships in the actual ledger, since parser observations are incomplete. Under the promotion lock, read the entire actual ledger, governing sources and actual body separately for every retirement before publication and after verifying that path is absent. Recheck the retained original bytes and mode before backup disposal, preserve unrelated state, and report each removal with its review and before/after identity in the per-file history. Any failed retirement check restores the prior package while retaining independent edits. Retirement review grants no extra scope or permission. Recheck candidate, source, project, target, review and factory identities before and under promotion; final in-place validation reads the retained original backup. Exclude only actual operation-owned temporary paths from project comparisons. Retain byte, file-mode, directory, attribution, privacy, scope, behavior and customization checks; stale inputs reject and final-check failure restores the prior package. Current reviewed no-op reruns remain distinct from accepted domain proof. The public factory `variant` and `validate-target` tasks prepare and type-check their declared checker runtime before consumers run; they cannot rely on dependencies left by a previous checkout or test. Preserve the native failure diagnostic when a checker cannot return its declared result. If this skill owns lineage maintenance, its independent runtime must expose a read-only plan and require the current ordinary file-review input before applying exact current derived metadata. Check the request and derived bytes before and after the effect under the shared lock and conditional-restoration limits; an effect-specific predicate must return explicit true. For catalog maintenance, `mise run mise-primitives-plan` freezes the selected-version schema catalog as exact `content_utf8` without changing this skill or the runner. Save those bytes unchanged. This skill's `mise run mise-primitives-update -- --review <request.json>` requires its review argument before its dependency chain starts. The argument names the ordinary file review that the writer validates at the effect. Its independent catalog writer rereads the entire current governing ledger, skill specifications and body around the effect, checks the exact request, prepared data and current source derivation, rejects missing or stale inputs, and uses the shared conditional-restoration limits. Retain every schema-derived primitive and preserve domain dispositions. The timezone-aware `checked_at` is a declared planning observation, not authenticated freshness or accepted judgment. The update still self-updates Mise at the required final maintenance phase; a changed version or schema invalidates the prepared review. Preserve native argument values, order and repetition through task references and standardization. A post-dependency may run after a started parent fails and does not supply permission or accepted output. If this skill owns lineage, reconcile the parent interface and its independent resulting-lineage review as well. Registry maintenance must expose every calculated formatted file, lineage and repository manifest before writing. The public refresh accepts all named skills but applies at most the next dependency-ordered file per reviewed call; replan and supply its current review until no changes remain, then run the full source and domain checks. Missing review input rejects before formatter execution. Body changes come first with their separate body-revision review. Each effect checks the complete current ledger, skill specifications, body, exact derived bytes, source inputs, file modes and request before and after; preserve earlier successful individual writes, report remaining work and conditionally restore only the failed current effect. This is not a multi-file transaction. A registry-owned manifest remains in its actual repository; only the trusted registry owner may select that exact related target while the skill body remains its review context. A request cannot select additional filesystem authority, and related targets cannot use bootstrap or body-revision modes. Preserve archived native identity, baseline attribution and target scaffolding. Native formatting calculates bytes without bulk --write. Use the selected Node permission controls against incidental writes and retain their stated limits. A configured but unavailable formatter or unbound `NODE_OPTIONS` blocks its consumer. Record the exact command and each consumer’s ancestor configuration candidates, including nested files, and recheck them at use. Dynamic configuration, plugin imports, environment and external effects still need current dependency review; native permissions alone do not prove hostile-code isolation or complete input coverage. Other writers and broader dependency acceptance still require integration. If actual authority explicitly permits a necessary prerequisite before integrated body validation, retain pending validation and use only the supported caller-selected pending-review path described in [the file-review procedure](examples/example-ledger-write.md#authorized-prerequisite-with-pending-body-validation). Keep SKILL.md-first design and outward implementation, all other guards and final acceptance. No review declaration grants that authority. For usage, load [the example](examples/example-ledger-write.md).

**Dependency contract.** Start at each promised result and work backward through what must be known, available, decided, observed, completed or remain true. Reach a verified input, accountable owner, authorized participant or explicit unresolved prerequisite before deriving order. Inspect purpose/acceptance, logical/semantic, reading/context, knowledge/evidence, creative/conceptual, people/judgment, data/assets, packages/environment, authority/privacy, time/events, capacity/shared resources, state/lifecycle and proof/downstream dependencies, plus actual domain additions. Record provider, consumer, type, condition, reason, owner, input/result, readiness evidence, validity, failure and recovery. Distinguish required, conditional, supported alternative, useful aid and scheduling preference. Support all-of, verified alternatives, conditions, required absence, continued availability and exclusive access at the actual protected moment. Definitions and authority precede interpretation; readiness precedes effects; adequate evidence precedes acceptance. File order alone is insufficient: apply this to every existing/new file, Markdown section, package, Mise task, Mastra step, script, resource and handoff.

**File graph.** Use all authorized specifications for this skill to determine requirements. Use the entire current Mermaid file graph as a replaceable baseline to reduce nodes and connectors, with SKILL.md as center, source and start. For a required whole-directory update, edit, update, replace or remove every existing file and add necessary files. Preserve required behavior and relationships before retiring owners. Work outward from SKILL.md, honoring actual prerequisites and every read, review and approval gate. For every baseline and added file, record disposition, obligation, actual change, intended and observed effect, validation, gap and next action in the existing ledger and changelog. Track intermediate inventories through final ownership or verified removal. Unjustified changes remain unresolved; investigate roles or replacements while advancing eligible work. Sampling, graph generation, bookkeeping and cosmetic churn do not prove implementation. Retain all other graph and acceptance obligations.

**Order and invalidation.** Preserve separate reading/design order, executable control flow, effect gates and acceptance gates. Discover runtime dependencies as well as static declarations; bind downstream work to the selected intermediate and its current review. A diagram or installed dependency proves neither sufficiency nor use. Schedule eligible independent work within actual authority and resource limits without duplicate prerequisites or competing writers. Distinguish invalid prerequisite cycles from versioned feedback with input version, purpose, expected progress, review, exit and existing budget. Exploration can use declared provisional inputs where authorized; downstream commitment and acceptance need their own prerequisites. Do not require a final creative concept before exploring it, impose a fixed idea count or universal process, or mistake exhausted time for quality. Changes to intent, evidence, project, source, asset, direction, software, authority, state or evaluator invalidate affected dependents only. Repair the lowest owner, retain independent work/customizations and recheck expiring conditions at use. During any skill creation or update with skill-factory, if a problem comes from how skill-factory works, pause that skill's creation or update. Trace the failure to the factory's owning rule, template, workflow or code; repair and validate skill-factory before resuming the affected skill. Use this ledger and per-file review/changelog protocol for the repair, invalidate affected plans and evidence, then replan and resume against the repaired factory baseline. Preserve the target's accepted state, independent work and variants, and existing authority; an unresolved factory repair blocks the affected consumer. Apply the controlled-improvement process to this skill, skill-factory itself and every skill the factory touches. At initial loading, read the complete current improvement contract and dimension catalog in [resource and experiment design](references/resource-and-experiment-design.md) through `mise run improvement-policy`, alongside the required body and ledger. Review outcome effectiveness, completeness, correctness, grounding, reasoning, calibration, generalization, deterministic mechanics, reliability, recovery, state integrity, speed, resource and cost efficiency, safety, security, privacy, human control, fairness, transparency, accessibility, usability, creativity, craft, semantic judgment, maintainability, portability, evidence quality and propagation; add every distinct source- or domain-required dimension without treating a finite catalog as universal. The last development step always checks for a real improvement opportunity; only a justified authorized trial with an adequate proof budget changes the skill. Freeze the complete accepted baseline, effective evaluator and inputs, protected criteria, measured resource conditions and stopping rule; test one named hypothesis and keep only a material gain with no protected regression. Treat an adopted trial as pending next-invocation confirmation: before relying on changed behavior, run its frozen regression probe, then compare that invocation's actual outcome at the final checkpoint. An attributable regression requires restoration of the pre-trial verified version before affected continuation; unknown safety or correctness blocks reliance while causality is resolved. Restore the complete owned change set, including modes and supported creation/retirement, through the existing reviewed writer or promotion owner. Compare baseline, candidate and current state; preserve independent edits and variants, resolve overlapping conflicts, retain evidence and history, and never use a broad reset or blind overwrite. Revalidate the restored consumer and invalidated proof. Missing safe restoration blocks adoption. Record no-op, pending, keep, discard, crash and restoration evidence at their existing status owners without turning declarations into acceptance. Improve this process by the same independent, held-out, end-to-end comparisons; the candidate cannot grade itself or weaken its evaluator. Preserve all per-file full reads and changelogs, authentic human evidence, authority and the mandatory final maintenance/delivery order.

**Meaning before implementation.** Identify competing readings that change behavior or acceptance, ground them in user intent and authoritative evidence, then define observable results and assessment questions. Retrieve facts; ask only material unretrievable choices. Define necessary terms, categories, actors, states, interfaces, decisions, dependencies and failures; label open versus closed enumerations. Use meaningful units, ranges, tolerances, timing and workloads from the user, sources or measurement, never invented precision. Bind each requirement to the exact operation, inputs, effects, criteria, evidence, allowed variation and failure/recovery. Define positive, negative, boundary and multiple-valid examples. Separately validate intended need, implementation and intended use, including evaluator calibration. Store resolved meaning with provenance, version and invalidation in the existing contracts; reopen only on material change. Select useful goal, quality-scenario, task/state, interface, failure, property/metamorphic or causal analyses from supported [domain guidance][domain-research-owner]; no catalog-filling formality or mathematical uniqueness claim for natural language.

**Tools and explicit resources.** Discover relevant available public tasks, managed executables, project commands, skills, host tools/apps/connectors, APIs, browser/computer capabilities and authorized preferences. Distinguish existence, capability, readiness and authorization; verify actual identity/version/schema, inputs, output, effects, target and context, including partial discovery and changed metadata. Honor explicit choices and mandatory tools, then established preference and grounded context after capability, privacy, compatibility and proof filtering. Installation alone is weak preference evidence. Label an inferred preference provisional, with its source, stable tie rule and easy correction. Preserve valid bindings and inspect actual results. Inference grants no installation, account, data collection, profile, transmission, state or permission authority.

Bind every instruction, step, rationale and check to the actual performing/governing tool or skill and its public invocation, arguments, resources, result, evidence and failure route. Pass long inputs as files/stdin with types, requiredness, path base, validation and actual consumer binding; avoid shell reinterpretation. Keep host-specific callable IDs, accounts, paths and model selection in integration.

**Evidence capable of the claim.** Separate the normative condition from the predicate and residual judgment. For each protected effect or acceptance claim, name the condition, alternatives, owner, inputs, validity and failure. Prove mechanical decidability where claimed; capture every material version, environment, clock, randomness, locale, ordering, mutable/network, concurrency and hardware input. Replay proves captured observations, not live readiness. Inspect the selected software contract and actual pass/fail behavior. Guard every relevant direct, alternate, retry, resume, import and maintenance consumer at the smallest capable boundary. Bind the exact consumed bytes, identities, target, operation, rules, inputs, evaluator and authority; use supported locking, atomicity, identity or transactions where needed and disclose residual races or bypasses. A recheck is not a transaction; conventions are not operating-system isolation.

Missing, malformed, contradictory, stale, unavailable, indeterminate or failed mandatory evidence cannot become success through defaults, warnings, callbacks, retries, caches or status translation. Test protected non-effects, preserved accepted state and legitimate recovery. Schemas, keywords, hashes, signatures, receipts, health and successful exits establish only their actual predicates, never semantic quality or user outcomes.

**Study/work matrix.** Every skill and invocation, for human and agent audiences, requires an accessible versioned study/work representation and matrix view, with actual relevant selection and use in research, framing, decisions and evals. The [use-case owner][use-case-contract] binds its explicit resources and responsibilities through public tasks and the domain workflow. Capture complete declared inventories from complementary [OECD fields](https://www.oecd.org/content/dam/oecd/en/publications/reports/2015/10/frascati-manual-2015_g1g57dcb/9789264239012-en.pdf), [ILO occupations](https://isco.ilo.org/en/isco-08/), [UN time-use activities](https://unstats.un.org/unsd/demographic-social/time-use/icatus-2016/), [O*NET](https://www.onetcenter.org/database.html) and [NIST AI use taxonomy](https://www.nist.gov/publications/ai-use-taxonomy-human-centered-approach), with versions, IDs, definitions, hierarchies, aliases, relations, scope, licenses and attribution. Paid occupations alone omit care, domestic, volunteer, learning, cultural and everyday work. Preserve unmapped, overlapping, interdisciplinary, unfamiliar and emerging concepts; add sourced extensions and retain unknowns instead of forced fits or future-completeness claims. Named disciplines and work families are open discovery prompts, activated by a concrete choice/outcome and evidence or labeled hypothesis, not universal tasks or predictions about individuals.

**Finite representation and coverage.** Preserve exact identity versus approximate correspondence, unordered membership versus meaningful order, repetition, grouping, roles, conditions and multiplicity. Represent arbitrary finite combinations, permutations and higher-order interactions among study/work concepts, people, context, outcomes, choices, evidence, constraints and judgments; a two-dimensional view must not restrict the underlying representation. Use captured-inventory selectors and lazy finite enumeration with explicit budget, count, continuation and interruption. Canonicalize unordered sets only, retain ordered/repeated meanings and comparator/locale/schema/version inputs, measure input-pool memory as well as output, and never silently truncate. Prefer existing records, standard iterators and simple queries over materialized global products, another database/language or per-cell model calls. Representability grants no usefulness, evidence, permission or test coverage. Keep vocabulary, representation, evidence and executed-test coverage distinct; unreviewed, missing, conflicting, failed, stale and supported non-use are separate states. Each invocation uses its relevant selection without loading every scientific matrix cell; the required full governing-ledger reads still apply.

**Human evidence and creative judgment.** Begin with the actual task, people, artifact/experience, purpose, medium, context, abilities, access, expertise, language, culture and time horizon, using authorized evidence without demographic inference or unnecessary sensitive data. Separate producer capability from recipient need and task success, comprehension, learning, comfort, aesthetics, emotion, trust, novelty and long-term outcomes. Map brief to study/work/context/outcome, inspect neighboring and missing concepts, ask which property changes which outcome for whom under which conditions, research support and counterevidence, and translate findings into concrete choices or testable hypotheses with transfer reasons and interactions. Record accessible source methods, dates, population, setting, task, comparison, measures, effects, uncertainty and limits; distinguish normative, causal, associative, theoretical, practical and inferred claims. Research at definition/material change and reuse current support on ordinary invocation. A source link or mechanism is not observed benefit in the current artifact.

Use actual authorized modalities and artifacts for synthesis, analogy, hypothesis, exploration, composition, visual/audio inspection, critique and revision. Programs prepare faithful facts, constraints, resources, comparisons and handoffs; models and assigned humans judge meaning, relevance, causation, originality, coherence, craft, taste and contextual quality. Preserve useful artistry, difficulty, ambiguity, surprise, emotion and multiple valid solutions within the brief. Do not substitute symmetry, minimalism, style, fluency, engagement, majority preference, random output or idea count for quality. Separate actual versus felt learning, physical taste/smell/touch versus images or simulations, measured visual constraints versus usability, neural association versus mental experience, local creative ratings versus diversity, collaboration versus best-component performance, and model forecasts versus actual participants. Apply research only within supported conditions; no universal beauty, diagnosis, intervention, surveillance or new-data authority follows.

Freeze domain criteria, observable defects, anchored examples/counterexamples and allowed variation before candidates. Calibrate evaluators with known valid, invalid and borderline artifacts; use matched, blinded or counterbalanced comparisons where feasible and report order, exposure, sample, subgroup, context and uncertainty limits. Human-predictive model judgments require appropriate held-out or actual human calibration, including order, verbosity, shared-bias and context sensitivity. Model agreement is not independent human confirmation. Separate artifact measurements, observed human outcomes, research-supported expectations and model/expert assessments. When required human or physical evidence is unavailable, complete eligible work and retain the exact unresolved claim for the authorized human-input route; never invent participants, responses, approvals, measurements, senses or probabilities.

**Preserve change and variant boundaries.** When creation, update, adaptation, import or refresh is in scope, apply the [generation contract][generation-contract] and shipped scope contract through `mise run validate`. Resolve source, destination, scope, project fit, dependencies, lineage and explicit authority before effects. Preserve independent source and variants, accepted customizations, license and attribution. User/project conversion changes actual inputs, instructions, resources, examples and proof; relabeling is insufficient. Explicit refresh compares recorded source baseline, current source and customization and resolves conflicts before writing. Keep sources unchanged unless in-place change was requested. No ordinary invocation grants migration, installation, synchronization or unrelated skill-edit authority.

**Reject purpose-defeating defaults.** Review every package surface for tempting patterns that fail a retained requirement or shift recipient work despite valid code, tests, elegance or local speed. Cover wrong task/audience, unnecessary generality, unsafe brevity, ornamental tool use, rigid creativity proxies, competing owners, hidden ambient dependencies, stale/cross-project state, failure-masking fallback, duplicate effects, false approval, misleading evidence, coupled variants and unreadable catalogs. Record context/temptation/violated rule/observable failure/minimum compliant alternative/owner/detection and real positive-negative examples at one maintained catalog owner. Distinguish definite defects from review signals. Put compliant behavior in actual defaults, templates, interfaces, examples and evals; isolate prohibited examples as data. Include legitimate counterexamples for useful abstraction, caching, concurrency, tests, documentation and human review. Never fix a conditional defect with a blanket ban or move a mandatory threshold after failure.

## Mise task graph

The [public task definitions][task-graph] own repeatable command entry points, executable versions, environment, builds and outer prerequisites. [Mastra][mastra-workflow] owns delegated domain steps, branches, joins, bounded parallelism, run state and supported suspension/recovery. Scripts own tested deterministic leaves; models own interpretation, synthesis, perception, creativity and judgment; humans own their assigned decisions and evidence; the host owns runner, tools, identity, communication and permission. Mise invokes Mastra once at an explicit boundary; Mastra directly invokes existing scripts or authorized runners in that prepared environment. Keep one effective scheduler, retry, cache, state and cleanup owner at each boundary and bound their combined resource/effect limits. Never create recursive Mise/Mastra/hook/process loops, competing graphs or duplicated effects. Preserve outer cycle, diamond, redundant/unknown/disconnected-edge and nested-task guards without prohibiting legitimate inner joins or versioned feedback.

**Public routes and missing bindings.** The scaffold supplies `mise run info`, `doctor`, `agentic-request`, `validate`, `test`, `lint-writing`, `lint-code`, `lint-placeholders`, `evals`, `domain-research-policy`, `use-case-policy`, `mise-primitives-policy`, `primitive-lifecycle-policy`, `task-graph-policy`, `decision-policy`, `invocation-policy`, `improvement-policy`, `ci` and `mise-primitives-update` through the [task graph][task-graph]. Each name is a `mise run <task>` route. Preserve its declared interface and add the actual {{NAME}} operation before acceptance. Bind the required ledger management entry and meaningful Mastra domain workflow to this graph and existing record owners. Their absence blocks dependent acceptance; a declaration is not an implementation. Keep private scripts behind public entries. Stop affected work if Mise or another required capability is unavailable.

**Outer task contract.** Every task declares prerequisites, including an empty list for a true root, and reaches CI, a named public operation or a required dependency. From each public entry, a dependency has one reachable outer-graph path. Reject cycles, diamonds, disconnected tasks, unknown edges and redundant transitive edges; never invoke nested `mise run` from a task. Required mutating acceptance waits for every prerequisite proof. Inspect effective includes, argument-bearing references, templates, `run` dependencies and scheduled/post-task semantics before claiming order. Derive concurrency, batching and caches from actual domain/resource needs; keep shared writers serial. Cache only reproducible work with complete input/version/environment/output declarations, never live judgment, mutable remote state, external effects or uncaptured randomness. Outer graph checks do not establish Mastra inner order or continued readiness at effects.

**Agentic request contract.** Use `mise run agentic-request -- --request <file-or-dash> --runner <executable> --runner-args-json <array>` with the [request template](assets/agentic-request-template.json). Bind request, completed use-case contract and exact promised outcome by digest. Operation and prompt use target-domain terms; prompt, every digest-bound skill-body dependency and open-ended primitive state domain role, outcome contribution, relevance reason and expected proof. The caller supplies runner executable and argument array outside untrusted request data. Dispatch long inputs through files or stdin without shell interpolation or secret-bearing arguments/environment. Validate structure, terms, files, digests, runner exit and actual output. These predicates never establish semantic relevance, actual use or sufficiency; assigned models or humans judge those claims.

**Verified input snapshots.** For `mise run agentic-request`, read each supplied file reference once, verify its digest on that exact byte sequence, decode UTF-8 without newline translation and consume that snapshot. The dispatcher first captures its own current SKILL.md as `body`, independently of request data or optional skill dependencies. A missing, nonregular, empty or invalid UTF-8 body blocks dispatch. The runner receives full verified `body.text`, `use_case.text` and `skills[].text`, with identities, digests and traced roles; prompts keep their exact decoded content. Reject duplicate JSON keys, non-finite numbers and malformed required shapes before dispatch. Supply only caller-authorized inputs. These transport checks do not prove complete initial-resource discovery, actual model reading, semantic relevance, fresh live state or isolation from concurrent changes. Required ledger/resources must still be explicitly loaded and used before their consumers.

**Required initial context.** The use-case `initial_context` array owns every required UTF-8 resource's `id`, `role` (`ledger` or `resource`), `binding` and `depends_on`. Require one governing ledger. A `package` binding (also the default) pins `path` and `sha256` relative to the contract. An explicit `invocation` binding omits those fields; the caller supplies the authorized path and current digest. Request `context` entries contain exactly `id`, `path` and `sha256`, resolving paths from the request file or stdin directory; they cannot change binding mode. Through `mise run agentic-request`, the [context reader](scripts/agentic_context.py) rejects malformed declarations, missing/extra/duplicate resources, invalid reading graphs, stale or wrong package bindings, and empty or invalid UTF-8 contents. It supplies full `context[].text` and binding modes in dependency order. Exact capture grants neither access authority nor semantic, matrix, model-use or domain acceptance. Complete those judgments before protected consumption.

**Selected software and features.** At implementation start verify the latest stable, non-draft, non-prerelease releases from official metadata, freeze exact versions and immutable revisions, and reconcile local, CI, runtime/minimum pins and compatible native package locks. A historical baseline is not a current claim. Use the target's package manager, declared platform/engine/peers/exports and actual resolved dependencies; tool locks do not replace native package locks or capture every system dependency. Inspect release-matched documentation, schemas, source, examples, tests and installed help for each consequential interface, default, failure and concurrency behavior. Keep stable, preview, experimental, deprecated and engine-specific support distinct. Experimental features need actual domain value, authority, explicit activation, tested limits and fallback or a blocker. Map the entire official Mise, companion and Mastra feature inventories to actual use, conditional trigger or evidenced exclusion at existing [primitive](assets/mise-primitives.json), [lifecycle][lifecycle-record], [decision][decision-record] and [resource](references/resource-and-experiment-design.md) owners. Installed or nominal features do not prove use.

**Implement software for this outcome.** Read the full public-to-consumer code path and every caller before applying the first adequate [Ponytail](https://github.com/dietrichgebert/ponytail) rung: omit unnecessary work, reuse existing code, standard library, native feature, installed dependency, one clear expression, then minimum correct code. Apply all six duties through this skill: simplify code, explain modes, review diffs, audit the tree, review shortcut comments and measure impact. The linked procedures are sources, not required installed skills or host plugins. Reconcile their relevant shared, native, example, version and adapter rules with this contract. Keep report-only and mode boundaries. No extraction grants installation, hooks, mode, dependency or edit authority. Avoid speculative layers, options, wrappers and tutorial-driven architecture; retain justified abstractions, safeguards, accessibility, hardware calibration, tests and mandatory methods. Record real tradeoff ceilings and upgrade triggers at the existing code/decision owner. Reviews use `delete:`, `stdlib:`, `native:`, `yagni:` or `shrink:` with location, replacement, preserved behavior and evidence; reviews/debt/benchmarks do not prove correctness or authorize unrelated edits.

**Verify every consumed language/interface.** Discover actual languages, dialects, embedded code, resolved runtimes/compilers/managers and platform constraints across scripts, configs, templates, examples, snippets, fixtures, tests and evals. Read current official version-matched language/standard-library/style/tool guidance, actual package manifests/locks/exports/peers/engines/adapters and relevant implementation/default/error paths. Record supplied capability versus the domain gap, material shapes/units/encoding/nullability/defaults/validation/errors/effects/state/concurrency/retry/cancel/cleanup. Preserve existing architecture/language unless requirements justify change. Manage executables through Mise and native dependencies through their own locks/public tasks. Shared Python helpers use isolated uv environments bound to Mise’s Python path, without ancestor-project selection or interpreter downloads. Preserve domain environments and reconcile customized runtimes. Test first, then typecheck/compile/parse and exercise real consumer behavior, error propagation and cleanup on representative, boundary, failure and recovery inputs. Unknown language or unavailable required tool remains an explicit gap, not an excuse for universal language claims or ambient executables. Reuse unchanged verified knowledge; invalidate changed consumed contracts and propagate the complete rules/checkers into independent outputs.

Keep code and Markdown at most 200 physical lines per file, including blank lines and comments. Each whole code construct, including functions, classes, interfaces and their contents, is at most 30 physical lines; block nesting across the whole file is at most 3. Genuine configuration files retain the size-limit exemption. Apply the same limits to tests. Prefer the standard library and real behavior; no shipped work markers, mocks, stubs or placeholder branches. Each script provides `--help` with usage, exit codes and an example, accepts flags/stdin without prompts, writes data to stdout and diagnostics to stderr, returns 0 for success, 1 for failed checks and 2 for bad usage, and reruns safely. Exercise these public contracts through `mise run test` and enforce applicable caps through `mise run lint-code`; those gates do not establish domain outcomes. `mise run check-runtime` installs locked dependencies before strict owned TypeScript checks; dependency declarations are excluded. `lint-code` then checks supported syntax, size and nesting; retain any unsupported language or construct check as unresolved, never as a pass. `test` depends on this chain.

For [Mise](https://mise.jdx.dev/), cover tools/backends/registry/install/resolution/verification/locks/platforms; configuration hierarchy/roots/profiles/variables/templates/environment; task forms/arguments/help/completion/tools/inputs/outputs; dependency construction/order/parallelism/failure/timeout/interruption/watch/hooks; includes/monorepos/workspaces/affected selection; caches/retention; diagnostics/schema/format/structured output/timing/docs; and applicable CI/IDE/shell/stubs/MCP/tool-skill/bootstrap/package/service/remote/OCI/plugin features. Verify the selected release: `depends` is a prerequisite set, explicit sequences establish order, `wait_for` does not schedule an absent task, and post-tasks do not establish preconditions or crash recovery. Inspect references inside `run`, inherited template dependencies and actual argument/environment forwarding. Task includes do not load model context. Fresh proof must disable both freshness skipping and task artifact reuse using supported controls, including `--force --task-cache off` where available. Trust, safe mode, child sandbox, host approval, redaction, encryption and platform enforcement are distinct. Do not let auto-yes, inspection side effects, absent vendor operations or skill-sync links imply permission, independent variants or scope enforcement.

**Five supported companions.** Every generated/updated contract supports [Packslip](https://packslip.dev/), [Usage](https://usage.jdx.dev/), [fnox](https://fnox.jdx.dev/), [Pitchfork](https://pitchfork.jdx.dev/) and [hk](https://hk.jdx.dev/) in their useful declared roles, with exact compatible executables managed through Mise and explicit per-output applicability. Required support does not force needless secrets, daemons, hooks or installation. Research all official features/configuration, source, versions, dependencies, failure behavior, platform limits and actual interfaces. A particular non-use must preserve this skill's declared support when its condition holds.

Packslip provides supported release metadata, platform/resource selection and byte verification. Prefer its backend only for releases with compatible manifests; a backend does not require an otherwise unused CLI. Check actual project/version/signer/issuer/platform/host/digests and required trust pins, age, expiry, sequence, withdrawal and rollback policy. Separate linked provenance from verified build provenance and signatures from quality or permission. Review version-bound vendor Usage/skill instructions before activation. Instruction-only skills need no fake binary; signing/publication and automatic synchronization need their own authority and preserve version, scope, lineage, license and attribution.

Usage owns one argument definition and supported parsing, help, completion, documentation and compatibility comparison; the skill owns actual domain forwarding and validation. Use supported Mise or verified script/spec integration, explicit roots/resources/scope/variants/stdin and native language behavior. Compare accepted and current specs through public tasks, including warnings and effect metadata; exporter limitations or a compatible spec do not prove implementation equivalence. Test spaces, Unicode, repetition, variadics, empty values, `--`, environment collisions and real consumer arguments without shell reinterpretation.

fnox resolves only necessary scoped credentials at the authorized consumer. Keep secret values out of prompts, arguments, packages, lineage, evals, logs, traces and ordinary caches; pass non-sensitive references. Prefer the maintained CLI with scoped subprocess execution. Declare config/profile/provider, required credentials, delivery and lifetime; missing required values or skipped leases must block despite warning defaults or ambient credentials. Verify fitting file delivery, short leases, proxy scope/host limits, expiry, renewal and cleanup; a cooperating proxy is not operating-system isolation. Cache, encrypted sync, shell/provider authentication and private key operations remain separately scoped.

Pitchfork owns required persistent process lifecycle, while finite work stays in Mise. Bind configuration, working directory, qualified project/worktree identity, ports, dependencies, daemon and probes to the pinned environment and declared lifetime. Reuse only the exact matching healthy instance. Prove bounded consumer-relevant readiness and application behavior separately; delay, PID and open port are insufficient. Verify actual probe combination semantics and use a combined gate for conjunctive readiness. Readiness and health can change during use. Define bounded retries, cancellation, logs and owned-child cleanup without touching unrelated services; startup, schedules, listeners, dashboards and proxies need actual purpose and authority.

hk selects exact files and dispatches existing language/domain checks or authorized fixes through public Mise. Pin its Pkl imports to the selected release; use the built-in evaluator unless a separate managed tool is actually required. Inspect effective configuration, roots, file selection and environment, including warn-and-fallback behavior and cache invalidation. A check can fix or stage under configuration; set intended effects and verify unchanged state for read operations. Authorized fixes preserve staged, unstaged, untracked and unrelated work, with declared write sets, conflicts and restoration. File locks cover neither undeclared nor separately privileged writers. Incremental checks do not replace full cross-file/behavioral CI. Hook installation and machine snippets are optional integrations; preserve the repository's read-only, path-confined MCP boundary.

**Required domain workflows.** Every accepted skill uses Mastra for its actual promised operation through public Mise, reusing existing leaf implementations and the caller-controlled [agentic dispatcher](scripts/run_agentic_request.py). A validation-only wrapper or unused dependency fails. Declare workflow purpose, route, inputs, outputs, context, step owners, gates and recovery at the existing owners. Preserve selected host models/tools, modalities, resources and creative freedom; no separate Agent, provider, account, model policy or hosted service is implied. Verify exact compatible runtime, package manager, published exports, peers, schemas, storage and engine. Type erasure and schema conversion are not runtime validation. Add only necessary language adapters and propagate actual compile/type/parse/test/discovery checks and locks into independent outputs.

Map the full Mastra workflow surface: construction/identity/schemas/register/commit/reuse/clone/inspection/nesting; sequence/parallel/joins/branches/mapping/iteration/concurrency/loops/waits; agent/tool/processor steps and criteria; runs/streams/events/observation/cancel/errors/retries/bail/callbacks/state; snapshots/persistence/pruning/storage/suspend/resume/restart/time travel/reexecution; dynamic definitions/registries/serialization/validation/schema flow; schedules/timezones/history/reconciliation/orchestrators/workers/deployment; and engines/server/client/MCP/observability/retention. Use only features with a concrete domain role, preserving mandatory workflow support. Verify actual branch matching, no/multiple matches and predicate errors; absorbed callback failures cannot be gates. Consume engine and typed domain statuses: suspended, waiting, pending, paused, cancelled, skipped, bailed or failed is not domain success. Verify actual input/output/state/context/resume validation and concurrency limits. Prefer independent parallel results with explicit merge to unproved shared state.

Persisted recovery, atomic resume, exactly-once effects and durable schedules require actual supported storage, host lifetime and crash/concurrency evidence. Retries, resume and time travel can repeat effects and never undo them; use appropriate idempotency, deduplication, transactions or compensation. Bind workflow/run/step/nested/iteration identities and skill/graph/schema/software/resource versions; migrate or block incompatible saved runs without changing suspended meaning or variants. Dynamic serialization may lose closures/options or conceal missing registries; test round trips, collisions, unresolved references and changed graphs, and retain code-defined flows where needed. Engines, workers and adapters have different supported guarantees; ephemeral processes are not durable scheduling. Resolve secrets at authorized leaves, verify filters/retention across state/snapshots/results/traces and require authority for exporter transmission.

**Human input and recovery.** Model: Choose the smallest authorized method that answers a material unresolved question: owner clarification/preference; participant evidence; qualified artifact review; audience comparison; authorized physical measurement; or consequential-action permission. These are different evidence kinds. Reuse valid answers and approvals. Prepare concrete artifacts, permitted checks, alternatives, criteria, uncertainty and effects before final approval; ask missing prerequisites before their consumers. Use accessible non-leading material and relevant people without universal sample counts, broad claims from one person, fabricated profiles or unnecessary repeated requests. Do not wait for exhaustive research to ask an already material question. Use realistic participant tasks for usability, accessibility, comprehension or learning claims.

Use the selected Mastra human-input/suspension contract and authorized host UI/tool for identity and communication. Bind review kind, expected person/role, workflow/run/step/nested path/iteration, exact artifact/input revision, required data, authority and validity. Return after suspension and stop before the first protected effect; suspension alone does not halt subsequent code. Keep safe preparation outside replayed effects, persist only necessary context with a verified lifetime and continue independent work. Silence, notification, timeout and defaults are not input. Validate provenance, schema, meaning and current bindings; route explicit accept, reject, revise, clarify or pending. Reject forged, duplicate, stale, replayed, wrong-target or unauthorized responses, serialize conflicts and resume the existing correct path. Revision changes authorized owners only and invalidates affected artifacts/proof/approvals; rejection cannot become success or repeated approval pressure. Handle cancellation, unavailable people, expiry, restart, storage failure and deadlines truthfully. Research/models/synthetic personas cannot replace real people or physical evidence, and human labels do not authenticate identity.

**Choose boundaries by evidence.** Before selecting workflow granularity, compare three isolated repeatable public-task implementations: A, the direct Mise/script control; B, meaningful Mastra domain steps reusing scripts and authorized runners; C, wrappers for every actual executable script route, accounting for helpers/tests without inventing executable workflows. Match complete inputs, tools, models, effects, outputs, checks and criteria across small, typical and larger success/failure/fresh/reuse/interruption/recovery runs. B and C must preserve the whole contract and mandatory Mastra. Measure setup/footprint/build/import/startup/process/serialization/snapshot/write/trace/log/CPU/memory/network plus accepted-outcome time, tokens and recipient effort, separating engine/model/external waits and repetitions. Choose the minimum complete demonstrated tradeoff, reject unnecessary or ineffective wrapping and preserve meaningful Mastra. If no design preserves every requirement, report the exact conflict before acceptance; documentation, compilation and SDK benchmarks cannot supply this comparison.

## Steps

`help` shows this command grammar, the frontmatter trigger and declared {{NAME}} operation. `info` runs `mise run info` and reports its actual JSON and exit status. `check` runs `mise run ci` and reports each job with its actual exit status. These diagnostic commands do not implement the promised {{NAME}} result. Define its domain operation in this body and the task graph before acceptance; add only necessary commands. For an unsupported request, report the missing capability or material input and stop its dependent work. Do not guess an operation.

1. **Frame and resolve.** Model: Apply the Outcome key, source/meaning and dependency contracts and select the supported {{NAME}} request. Before the first real task, apply an installed starting-point skill when available; absence alone does not invent a blocker. Resolve scope through the shipped scope contract and `mise run validate`. Freeze purpose, boundaries, mandatory methods, allowed variation, conditions and acceptance before coding. Ask only for a material unretrievable fact or decision; block its dependents and continue eligible work. Ground terms, roles, study/work selection, criteria and counterevidence through `mise run domain-research-policy` and `mise run use-case-policy`, with actual resource contents supplied to the consumer. Mere domain keywords cannot prove relevance. Keep one numbered workflow in dependency order. After every step, record progress and fresh evidence externally at the invocation/ledger owner.
2. **Inspect and prepare.** Model: Read all governing instructions and the full current package/history/callers/copy paths, record immutable source and owned-byte baselines, verify chosen tool readiness through `mise run doctor`, and resolve real software/interfaces/environment before effects. Map every aspect, primitive, task and lifecycle phase from discovery through retirement to its actual outcome, reason, owner, progress, proof and failure. Reject collisions, symlinks, unknown owners, hidden host dependencies and unresolved source/customization conflicts before writing. For existing work, preserve triggers, purpose, scope/audience, accepted behavior, examples, eval baseline, evidence, boundaries and mandatory methods; a shape repair cannot replace domain meaning.
3. **Reconcile the governing ledger.** Model: use the public ledger entry and real Mastra read/review workflow to read all authorized governing documents, the entire ledger and current body. Reconcile source-to-ledger and ledger-to-source coverage, every file role, semantic facet, typed relationship, applicability, dependency and body/initial-load route. Retain omissions, unknowns, conflicting evidence and frozen acceptance/failure criteria at their owners. A mandated complete body rewrite first finishes and proves this source/ledger/infrastructure foundation, then authors a true replacement reconciling all current contents. Validate complete meaning, unique ownership, dependency order, initial loading, syntax and resource routes against the whole ledger before later file development. This initial contract validation permits eligible implementation. For dependency-ordered creation before a target body exists, supply the already reviewed candidate through the guarded bootstrap path. Its review must bind the current ledger, unchanged source and exact candidate bytes; read the complete candidate and review before and after each individual write. An installed body or body alias disables that path. Initial review records remain declarations with full acceptance pending. For SKILL.md installation or replacement, use an explicit `body_revision` with a separately retained previous-body snapshot or declared absence and the candidate review. The prepared `new_file` supplies the candidate. Bind previous and candidate bytes to the current ledger and source; reject stale review, aliases and source/snapshot overlap. Read the actual installed body before replacement, the candidate after installation, and all governing inputs and retained review around the change. Preserve modes and independent edits. Review declarations do not authorize effects or establish semantic or human acceptance. Human: supply required clarity and same-meaning review before final acceptance through the Human input and recovery protocol. A model review, push, mechanical read or initial validation cannot supply that human evidence or final acceptance.
4. **Freeze the executable change plan.** Model: freeze the actual public operation, ordered steps, one owner per responsibility, inputs/resources, effects, readbacks, rejection/recovery, tests and propagation in the existing plan/decision records. Resolve the change and variant boundaries above, including source/destination, audience/scope, source/customization conflicts, lineage and authority before effects. Preserve independent work. Use `mise run task-graph-policy` for the outer graph and the actual domain workflow for inner gates; neither proves the other. Missing ledger management or meaningful Mastra binding blocks dependent execution/acceptance. Expose the versioned plan before action, record explicit replanning after material change, and invoke the Human input and recovery protocol before any step needs an unretrievable material fact or authority. Continue eligible independent work. Name the actual reader, program, workflow, model, human and host at each step, with explicit control-flow labels. Readback: the bound plan and permitted effects; a log does not prove correctness.
5. **Implement the selected operation.** Mastra: execute the frozen domain plan with the existing scripts and authorized runners; use the preceding human protocol at each protected boundary. Model: Use BOOTSTRAP, RED, GREEN, REFACTOR. For a new skill, author public Mise tasks, CI/graph contract tests, failing behavior tests, implementation, documentation and evals in that dependency order. Fix the lowest responsible owner and every affected caller/copy; preserve valid alternatives. Deterministic mechanics belong in tested leaves behind the public task/domain workflow. Stop decomposition where code would distort meaning, remove required creative freedom or substitute a weak proxy for perception or judgment. The assigned model or human performs that work and records its limits.

   For every individual added, changed or removed file, including repeat writes, capture all actual ledger, current body and authorized governing input bytes before and after the change and check their integrity. First complete semantic review of every governing clause and relationship. Review every changed or affected record before and after the change, including callers, dependencies, conditions, exceptions, authority, evidence and body integration. Reuse only a completed review whose exact source and relevant context remain valid; unchanged bytes alone cannot prove unchanged meaning. Missing coverage, stale dependencies, missing relationships or unknown affected scope require broader semantic review, up to the whole governing packet. The pre-review determines the necessary change, owner, prerequisites and proof; the post-review checks its actual effect against all retained rules and invalidates affected evidence. Fix violations or safely restore before dependent acceptance. Parsing, hashing, summaries and prior PASS labels never supply semantic judgment. Other required full reads retain their scope. After every individual file change, append a durable entry to the existing per-file change history in task or invocation state. Record the exact file and before/after identities, what actually changed, its intended contribution, why it was needed, the exact request or source-rule locator/quote supporting that reason, the post-read findings, validation and remaining proof. Keep facts, reviewer judgment and pending acceptance distinct. Use this history throughout execution to choose the next unfinished prerequisite, find the current owner and last valid evidence, avoid duplicate work or repeated failed approaches, target necessary checks and propagate invalidation. Reconcile history with the current ledger and actual files before relying on it; preserve every required full read and integrated check. The ledger remains the rule owner, and new log entries or completed edits alone are not goal progress or accepted outcomes. These reads validate the exact change, not a batch or only a hash. Trace all functional roles, source rules, relationships, dependencies, consumers, evidence and outcomes. Decide whether the change affects core behavior, required initial context or the public contract; if so, revise the owning body clause and reference. Keep all core and most important operating rules directly in the body. Never depend on a private factory prompt, installation or shared run state.

   Record actual contribution, owner, changed bytes, rationale, body decision, effect, proof, uncertainty and invalidation at the existing ledger owners. A support-file-only change cannot hide a core change. Preserve accepted state and unrelated work; reject stale body/content or missing mandatory reads before dependent acceptance.

   Select the one public {{NAME}} task that fits the resolved request and follow its declared Mise-to-Mastra-to-leaf or authorized-runner path. Preserve direct vision, current web research, browser/computer interaction, tool calls, multimodal inspection, reasoning, synthesis, creativity and human-sense judgment whenever the outcome needs them. Use tested deterministic mechanics for stable manifests, parsing, validation, state, coverage, timestamps, digests, budgets, measurements, comparisons and rollback evidence. Repeatedly separate mechanics from judgment until further decomposition would distort meaning or remove needed capability; record why code cannot supply the remainder. A schema pass, term match or deterministic envelope cannot replace direct judgment or narrow the caller's authorized capabilities.
6. **Prove the integrated result.** Mise: run the applicable public policies and `mise run ci`; execute actual domain evals and independent invocation. Model: inspect actual artifacts, required modalities, source-to-claim support and recipient outcomes against frozen criteria. Human: perform assigned recipient tasks and authentic reviews using the preceding protocol. Readback: fresh exact inputs/revisions/bytes/dependencies, observed domain results, protected non-effects, preserved state, legitimate recovery and capable judgments with limits. Consume warnings, exit status and domain result separately. Repair failed owners and invalidate affected dependents. A package check or zero-exit result cannot replace actual domain success.
7. **Account for the invocation.** Model: account for every published task using `mise run invocation-policy -- <receipt>`. Retain actual command output for each run and source-supported request-specific reasons for other tasks, including the accounting task itself. Readback: complete task accounting distinct from acceptance. Only after required work is accepted, follow the optional improvement contract in Evals through `mise run improvement-policy` and its actual domain experiment. Keep the protected baseline and genuine keep/discard/crash evidence outside the editable candidate; restoration and its digest must be verified. Always perform the controlled-improvement opportunity and prior-adoption checkpoint defined in the order/invalidation contract before final maintenance and delivery.
8. **Maintain and deliver.** Model: for package changes, refresh accepted lineage through its declared public owner and verify clean independent invocation and source preservation. After outcome acceptance, perform the mandatory final maintenance in Evals through `mise run mise-primitives-update`, then rerun `mise run ci` under the resulting version and record actual results. For all work, prove recipient handoff and authorized delivery; for publication, inspect installed/discovered bytes and authoritative remote revision/CI/content. Preserve author/license/version/lineage and private data. A remote update grants no unrelated tagged release, signing or merge authority. Readback: the accepted actual delivered contents and required checks for that revision. Finish only at full acceptance; failures, unmerged proposals and blocked delivery remain incomplete.

## Assets

The body remains the central development and initial-load owner. Read the [generation contract][generation-contract] and [use-case contract][use-case-contract] before generation/update acceptance through `mise run validate` and `mise run use-case-policy`. Load the [lifecycle][lifecycle-record], [decisions][decision-record] and [decision rationale](references/decisions.md) for resource/owner/change decisions through their public policy tasks; append one dated rationale per accepted decision. Load the [domain guide][domain-research-owner] through `mise run domain-research-policy` before defining or changing meaning, evidence, matrix selection, roles or outcomes. Load the [resource/experiment guide](references/resource-and-experiment-design.md) and [improvement contract](assets/improvement-contract.json) through `mise run improvement-policy` before choosing data structures, formats, caches, benchmarks or experiments. Load the [writing rules](references/writing-rules.md) through `mise run lint-writing` before prose changes. Actual reading and use remain required independently of checks.

Load the [first-run example](examples/example-first-run.md) through `mise run validate` before first use or when a complete-run reply is needed. Ship one actual complete run per supported command plus the likely failure, naming the guess each removes; record user words, visible reply, every command, real output, exit code and full created file contents. Update affected examples in the same commit as a changed contract, path, schema or command. Never invent transcripts. Load the [eval case template](assets/eval-case-template.json) through `mise run evals` when adding cases and the [invocation receipt template](assets/invocation-receipt-template.json) through `mise run invocation-policy -- <receipt>` for complete task accounting. Run behavior checks through `mise run test` after changes; every public implementation interface stays behind its declared task.

Propagate every applicable rule, relationship, owner, dependency, language, audience, matrix, human-input, tool/resource, workflow, companion, body/load, failure/recovery and proof duty through actual generation, copy, update and refresh paths. Extend existing records and checkers; compare current canonical versions as well as copies, because identical stale copies fail. Preserve customization through explicit comparison. Every output embeds the operative ledger method in its own domain-complete body, ships its own authorized ledger/resources/checks and declares runtime, packages, workflows and host runner. Prove clean independent use without factory installation, private source packets, host paths, undeclared credentials, caches or shared run state. Discover domain dependencies; copying factory labels is insufficient.

## Evals

Use [skill-local evals](evals/evals.json) and [trigger queries](evals/trigger-queries.json) through `mise run evals` and the actual domain evaluation path; registry publication also requires the complete artifact set in the generation contract. Preserve the full source denominator and parent conjuncts, including requirements outside a case register. Keep at least four realistic domain cases with prompts, expected output and verifiable assertions, plus positive/negative triggers and near misses. Replace every seed. Freeze fixtures, criteria, expected failures, allowed variation, evaluator and comparison before implementation. Test required/forbidden behavior, boundaries, failure/recovery, with/without-skill or prior-skill conditions and repeated cold/warm runs. Schema checks do not execute cases: actual agents must load required body/resources, use real tools and choose compliant actions under tempting alternatives. Use authorized capable and less-capable models with fresh contexts and repetitions; report identity, counts, failures, variation, independence and limits outside portable content where required.

**Diagnostic summaries.** `mise run validate` reports `PASS {{NAME}}: 0 problems`; `lint-writing` and `lint-code` report `checked files, 0 problems`; `lint-placeholders` reports `checked files, 0 placeholders`; `evals` reports `eval checks: 0 problems`. The `improvement-policy`, `domain-research-policy`, `use-case-policy`, `mise-primitives-policy`, `primitive-lifecycle-policy`, `task-graph-policy`, `decision-policy` and `invocation-policy -- <receipt>` routes report, respectively, `improvement contract`, `domain research`, `use-case contract`, `Mise primitives`, `Primitive lifecycle`, `task graph`, `decision records` and `invocation receipt`, each followed by `: 0 problems`. Each route is a public `mise run <task>` invocation. File counts may grow; problem counts must remain zero. These summaries establish only their named predicates. Untouched scaffold placeholders must fail; no summary proves the domain result.

**Protected consumer and propagation proof.** Use isolated disposable real packages, declared test credentials and real public paths. Inject omitted/weakened rules, malformed/duplicate keys, wrong scope/audience/owner/target, unsupported alternatives, missing/ignored/misrouted resources, stale copies/evidence/intermediates, changed reviewed bytes, conflicting writers, unsupported versions/configuration and direct/retry/resume bypasses. Prove the dependent effect/acceptance does not occur, prior accepted state and unrelated work survive, and valid recovery permits the legitimate result. Include zero-exit false domain results, absorbed predicate/callback failures, optional/advisory gates, stale caches, absent validators, false human approval, missing matrix use, incomplete first/later loads and arbitrary support-file core changes with stale bodies. Test full captured deterministic repeats separately from fresh observations/judgment, all/alternative/conditional/exclusive/continued-validity gates, invalid cycles, valid joins, bounded feedback and eligible independent work. State the actual enforcement perimeter and residual races; a privileged bypass outside it is not controlled by a convention.

**Domain and interface proof.** Exercise every affected domain invocation and package create/update/adaptation/import/refresh/maintenance path. Cross human/agent audiences with user/project scope, legacy inspection, changed audiences and secondary consumers where applicable. Prove actual project fit, independent source/variant behavior, safe reruns, three-way customization conflict handling and user-scope use in two materially different projects. Test explicit versus inferred tool choice, changed/wrong/partial tools, actual resources/arguments/effects, mixed data/instruction handling, missing judgments and component-pass/domain-fail cases. Check real selected-release Mise ordering/cache/environment/trust behavior, companion signer/bytes/parser/secret/hook/readiness/lifecycle boundaries, Mastra branching/joins/iteration/schema/status/storage/replay/cancellation/human identity and any adopted dynamic/schedule/worker behavior. Verify source/interface/runtime/component/integration/domain proof separately, including language discovery, valid-but-wrong software, incompatible guidance, unsupported APIs and unknown languages. Required support is not proved by a package name or reputation.

**Human, representation and quality proof.** Test exact/approximate concept identity, versions, aliases, extensions, unknown/conflicting evidence and actual independent matrix resources. Exhaust small bounded selections with independently known counts/results for order, repeats, grouping and higher-order interactions; test larger symbolic spaces with explicit limits/continuation and justified interaction coverage, never pairwise-as-exhaustive claims. Include positive/negative/conflicting research transfer, multiple-field/multiple-activity cases, order/repetition, unfamiliar work, irrelevant proposed fields and unavailable physical capability. Inspect real visual/perceptual/cognitive, learning-versus-ease and creative audio/narrative/physical or multisensory cases as applicable, preserving multiple valid artifacts. Reject polished wrong answers/charts/code, unsupported citations, valid-but-unusable outputs, hidden recipient work, false sensory/human claims, universal taste scores and creativity-excluding rubrics. Observe actual agents choosing between tempting anti-patterns and compliant alternatives; report false positives, misses and unresolved semantic judgment. Required real human preference, performance, expert review and permission retain their own protocol and limits.

Run complete representative domain cases through independent validation and invocation, actual outcome inspection and recipient use. For generation/update capability, also prove deterministic, creative and applicable service/credential skills, both scope variants and clean independent operation. Every representative requires meaningful Mastra; cover useful roles of the five companions across the suite without compulsory irrelevant use in each case. Trace vague input to grounded criteria, selected user tool, explicit resource, public Mise, Mastra, script/model/human action, actual result and adequate acceptance, including creative judgment before and after. Scaffolds, schemas, signatures, compilation, health, lint, catalogs and worker completion do not replace the result. Calibrate graders against valid/invalid/borderline and held-out cases where feasible, investigate disagreement and report finite coverage rather than universal correctness or deterministic model behavior.

**Efficiency and optional improvement.** Close the smallest ready functional path after its required gates and prerequisites through its required validation and authorized delivery while keeping independent eligible work moving. Tie infrastructure, ledger maintenance, reporting and packaging to a concrete unmet obligation, its actual consumer and the gap that existing mechanisms cannot fill. Do not repeat supporting work on unchanged inputs while a required functional path is ready. Reuse still-valid proof only with current artifact, dependency, environment and evaluator bindings and no required repetition; run affected checks plus every required integrated check. If progress stalls, use actual failure and timing evidence to change the ineffective path without dropping scope or proof. Report implemented behavior, validated behavior and accepted obligations separately. State the denominator and weighting behind a measured completion percentage; label an informal estimate and its uncertainty. Preserve every required full ledger/source/body read, source judgment, human proof and purposeful creative iteration. Keep speed claims pending until matched accepted end-to-end trials support them. Measure skill invocation, complete creation/update and downstream output invocation separately from request/declared entry to accepted outcome, including discovery/readiness/context, work/waits, validation, retries, repair, delivery and recipient use/effort. Preserve necessary review and creative iteration. Use repeated matched cold/warm baselines, actual phase/total timing, token/call/cache/reasoning counters and applicable CPU/memory/storage/network/process/concurrency/accelerator/cost/attention measures; label estimates, missing counters, external waits, incomplete cases, model/concurrency bias and generalization limits. Separate single-attempt success, eventual retry success and all-observed-trial success. Optimize the measured critical path with verified reuse, batching and independent work without omitting required full ledger/source/body reads. Parse each required full read once per boundary, preserve exact locators/defaults/exceptions and avoid materialized candidate products. Component timing, characters, wrapper counts or upstream claims do not establish end-to-end savings.

Only after required work is accepted, run an optional bounded improvement through `mise run improvement-policy` and its owning domain task. Freeze a fresh accepted baseline, evaluator, fixtures, environment, time/repetition limits and protected dimensions. Change one named dimension at its smallest owner; record exact content digests, keep/discard/crash, actual results and supported resource non-use outside the editable candidate. Keep only a material measured improvement with no protected regression; otherwise restore the accepted bytes and verify them. No open-ended loop unless the user's completion condition requires one. Keep the selected Mise version fixed during outcome work; after acceptance run `mise run mise-primitives-update`, reconcile the full catalog, pins and compatibility, then rerun `mise run ci`. A package-manager/update failure stays blocked; never force replacement.

**Completion and report.** Fresh required policy, package, repository, target and invocation checks must pass, with exact tested revisions/bytes/source/inputs/dependencies/environment/evaluator/authority and inspectable supporting evidence. `mise run doctor` must report actual readiness; exit zero and zero-problem summaries prove only their named checks. Require source-to-claim review, domain-swap and domain-term-removal attacks, real outcome and recipient use, omission/rejection/recovery proof, independent outputs and verified installed/authoritative remote delivery. No mandatory missing, failed, stale, blocked, skipped, indeterminate or unproved item can be averaged away or dropped. Report location, issue, change, requirement, owner and evidence; preserved purpose; key/ownership/reading route; dependency discovery/order/readiness/independent work/invalidation; meaning and variation; tool/resource/version/feature maps; workflow/engine/A-B-C/recovery; language/Ponytail/interface choices; matrix's four coverage classes; real human contributions and limits; anti-patterns and legitimate counterexamples; timing/tokens/recipient effort/scalability; variants/lineage; and remaining gaps. A blocker, local-only change or finished subtask is not completion. Stop at the first fully accepted state and add no unrelated work.

[use-case-contract]: assets/use-case-contract.json
[lifecycle-record]: assets/primitive-lifecycle.json
[decision-record]: assets/decision-records.json
[domain-research-owner]: references/use-case-specificity.md
[generation-contract]: references/generation-contract.md
[task-graph]: mise.toml
[mastra-workflow]: https://mastra.ai/docs/workflows/overview
