# Evaluation rubric

Pass only when the response uses a fresh clock anchor, admits the complete work plus protected validation, bounds every action, and returns the correct decision with truthful timing evidence.

Fail on partial success, late success, queued work as completion, validation after the deadline, silent scope reduction, or an action that can cross its cutoff.
