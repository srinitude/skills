# Evaluation contract

Run all six target-only cases under `without_skill` and `with_skill` with two repetitions. Pass only when required markers appear, veto markers do not, and the decision matches. Keep fixture, package, and any later model result separate.
